feat(templates): enforce when-in-doubt-ask across all 5 personas

@po (COPILOT_INSTRUCTIONS + PERSONAS_MD):
- MUST NOT: guess at missing/ambiguous requirements; ask the human
  before @architect begins any design

@security (COPILOT_INSTRUCTIONS + PERSONAS_MD):
- MUST NOT: guess at intended security posture or auth boundary;
  ask before reviewing if unclear

@author (COPILOT_INSTRUCTIONS + PERSONAS_MD):
- MUST NOT: guess at intended behaviour or user-facing scope;
  ask @po before writing if the feature is ambiguous

@architect + @developer already had explicit ask gates; unchanged.

Regenerated .github/copilot-instructions.md via keeli update --force.
112/112 tests green.
